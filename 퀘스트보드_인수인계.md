# 퀘스트 보드 — 인수인계 문서

**현재 버전 v5.9 · 빌드 2026-09-16 21:40**

다른 AI(또는 다른 세션)가 이 문서만 읽고 바로 이어서 작업할 수 있게 쓴 문서다.
사용자는 **이준희**, 아이센스 IT사업부 PM/QA. 이 앱은 본인이 매일 쓰는 개인 도구다.

---

## 0. 작업 시작 전 반드시 할 것

1. **현재 소스를 받는다.** 원본은 GitHub 저장소에만 있다.
   `https://raw.githubusercontent.com/syrano15/quest-board/main/index.html`
   → 파일이 360KB 가까이 되어 **WebFetch 로는 잘려서 못 읽는다.** 사용자에게 현재
   `index.html` 을 첨부받거나, 직접 다운로드해서 읽어야 한다.
   오래된 사본을 고쳐서 올리면 그동안의 기능이 전부 날아간다. 실제로 사고가 있었다.
2. 이 문서와 소스의 버전이 다르면 **소스가 정답이다.** `APP_VERSION` 을 먼저 확인할 것.
3. 작업 규칙(사용자가 명시한 것):
   - 답변 **맨 위와 맨 아래에 버전을 모두** 적는다. 위에만 있으면 스크롤을 올려야 해서 싫다고 함.
   - **작업 도중에 "이것 좀 확인해주세요" 같은 요청을 끼워 넣지 않는다.** 다 끝내고 한 번에 정리해서 준다.
   - 간결하고 실용적으로. 격식 없는 한국어. 문서화 과잉 금지.
   - 산출물에 바탕·명조 계열 폰트 금지. 고딕만.
   - **매 작업마다 결과물 + 이 인수인계 MD 를 같이 첨부한다.** (2026-09-15 추가 요청)

---

## 1. 이게 뭔가

하루 할 일 관리용 **단일 HTML 파일 PWA**. 빌드 도구 없음. CSS·JS 전부 인라인.
외부 의존은 Google Fonts 하나뿐(그것도 지연 로딩).

- 배포: GitHub Pages — 저장소 `syrano15/quest-board` (**Public**)
- 주소: `https://syrano15.github.io/quest-board/`
- 저장소 파일: `index.html` · `manifest.json` · `sw.js` · `icon-192.png` · `icon-512.png` · `icon-512-maskable.png`
- **v4.12 부터 `index.html` 하나만 올리면 된다.** `sw.js` 는 손대지 말 것.
- 배포 방법: GitHub 웹 편집기에 `index.html` 전체를 붙여넣기. 그것만 씀.

### 보드 두 개
`회사`(앰버 테마) / `집`(퍼플 테마). 목록은 따로 저장되고 앱 안에서 전환한다.

### 데이터가 사는 곳
- 브라우저 `localStorage` — 회사 `questboard:v2`, 집 `questboard:v2:home`
- 기기 간 동기화는 **본인 계정의 비공개 Gist**(description `questboard-sync`, 파일 `work.json`/`home.json`)
- 서버도 DB도 없다. 공개 저장소에 있는 건 빈 껍데기 앱뿐.
- 한 문장 설명: "코드는 GitHub Pages 에 공개, 데이터는 각 기기 localStorage,
  기기 간 동기화는 내 계정의 비공개 Gist 경유."

---

## 2. 데이터 구조

```js
store = {
  days: {                          // 날짜키 "2026-09-15" → 항목 배열
    "someday": [ ... ]             // ⚠ 언젠가 목록이 가짜 날짜키로 들어 있다 (아래 참고)
  },
  dismissed: {},                   // 날짜별 "오늘 시작" 안내 닫음 표시
  cats: [{id,name,color}],         // 카테고리 (보드별)
  tomb: { id: {k, at} },           // 삭제 묘비, 60일 보관
  weights: { "2026-09-15": {v,at} },
  works: {                         // v5.0 추가 — 작품 등록부 (이름이 키)
    "소닉 제네레이션즈": { kind, sea, tot, fin, finAt, at }
  },
  meta: { dayAt: {}, catsAt: 0 }
}
```

### 항목(카드) 하나
```js
{ id, text, cat, ord, at, done, doneAt,
  note, time, from, to, pri,      // pri 0/1/2 = 보통/중요/긴급
  seq, seqTot,                    // 회차 (드라마 3/12)
  defer, everDefer, canceled, reason, pushed, color,
  by,                             // 언젠가 항목의 목표 시기
  wsrc, wadd }                    // 언젠가에서 꺼낸 날 / 담은 날
```

### 성공·실패 (보드 공통, 별도 키)
```
questboard:habits  = [{id,name,desc,ord,mode,noask,amt}]   mode "wd"=평일만 "all"=매일
questboard:hres    = { "2026-09-15": { hb3: {v,at,why,amt} } }   v 1=성공 0=실패 2=예외
questboard:habat, questboard:habseed, questboard:habfresh
```
- `why` = 실패·예외 사유 (v5.2), `amt` = 금액 원 단위 (v5.3)
- `noask:1` → 사유 안 물음 / `amt:1|0` → 금액 칸 켜기·끄기 (없으면 이름으로 판단)

### 기타 localStorage 키
```
questboard:profile   questboard:ver      questboard:verlog   questboard:theme
questboard:who:<보드>  questboard:daystart  questboard:wishopen  questboard:wishbak:<보드>
questboard:rowsize   questboard:cols     questboard:sync:<보드>  questboard:idle
questboard:clockface  questboard:clockopt   (clockopt: "s"=초, "h"=12시간, 둘 다면 "sh")
```

---

## 3. 함정 — 여기서 사고가 났다

### (1) 언젠가 목록이 `days.someday` 안에 있다 ★ 가장 위험
`"someday"` 는 날짜가 아니라서 `Date.parse` 가 NaN 이다. 그래서:
- 항목 `at` 이 `1` 로 찍혀 **어떤 묘비보다도 오래된 항목**이 되고, 동기화 한 번에 통째로 사라졌다.
- 병합에서 묘비 하나가 몇 달치 아이디어를 죽였다.
- 구버전 payload 가 someday 를 통째로 덮어썼다.

**v4.13 에서 세 경로를 전부 막았다:**
```js
base = store.meta.dayAt[k] || Date.parse(k+"T00:00:00") || (k===SOMEDAY ? Date.now() : 1);
if(k !== SOMEDAY && t && (t.at||0) >= (it.at||0)) return;   // 언젠가엔 묘비 미적용
if(k === SOMEDAY && loc.length && !out.length) continue;    // 병합으로 비면 손대지 않음
```
**이 방어막들은 예외 처리지 근본 해결이 아니다.** 병합 로직을 건드릴 때마다
"언젠가는 괜찮나?" 를 다시 확인해야 한다. 제대로 고치려면 `store.wish` 로 분리해야 하는데
마이그레이션이 필요해 미뤄뒀다. (사용자도 알고 있고, 급하지 않을 때 하기로 함)

### (2) 한국어 부분일치로 추측하지 말 것 ★ v5.4 에서 크게 데임
v5.3 까지 작품 종류를 이름으로 찍어줬는데 전부 틀렸다:
```
"세훈이 오후 산책"              → /책$/     → 책
"…신규 프로젝트 클라 관련 문서 읽기" → /읽기/    → 책
"핫 레스토랑 설치 및 플레이"      → /플레이/  → 게임
```
한국어는 낱말이 붙어 다녀서 부분일치가 늘 사고를 낸다.
**v5.4 에서 추측을 전부 없애고 사용자가 직접 고르게 했다.** 되돌리지 말 것.

### (3) 서비스 워커
- `respondWith(hit)` 로 넘긴 Response 는 본문이 소비돼 나중에 `clone()` 이 실패한다.
  비교용 사본은 **넘기기 전에** 떠 둬야 한다.
- `navigator.serviceWorker.addEventListener("message")` 는 `startMessages()` 를 불러야 전달된다.
- 캐시 이름은 버전 없는 고정값 `questboard-shell`. 그래서 `index.html` 만 올려도 반영된다.

### (4) 기본 항목이 진짜 목록을 덮어쓰는 문제
오래 안 켠 폰이 성공·실패 기본 항목으로 원격을 덮어썼다.
`habFresh`(기본값 그대로 = 손댄 적 없음) 표시로 막았다. 같은 패턴을 다른 목록에도 적용할 것.

### (5) 버전 문지기 (v4.15)
동기화 payload 에 `app: APP_VERSION` 을 싣는다.
- 원격이 **나보다 오래됨** → `addOnly` 로 더하기만. 덮어쓰기·묘비·카테고리 교체 금지. 정리본을 내가 올림.
- 원격이 **나보다 새것** → 받기만 하고 **올리지 않음**. 새로고침 안내.
- ⚠ `older` 분기는 early return 이다. **새 동기화 필드를 추가하면 이 분기에도 넣어야 한다.**
  실제로 v5.1 에서 `works` 가 빠져 있어 폰의 엔딩 기록이 사라질 뻔했다 → `mergeWorks(r)` 를 양쪽에 넣음.

### (6) 문서 캡처가 버튼보다 먼저 돈다
대기 화면은 `document` 에 캡처 단계로 "아무거나 누르면 닫기" 를 걸어뒀다.
캡처는 document → … → 버튼 순서라, **버튼에서 `stopPropagation` 을 해도 이미 늦다.**
안에 둘 버튼(달력 화살표)은 document 핸들러 쪽에서 `e.target.closest(".zch")` 로 걸러내야 한다.

### (7) 대기 화면을 여는 버튼은 한 박자 늦게 열어야 한다
"아무거나 누르면 닫기" 가 document 캡처에 걸려 있어서, 버튼의 `onclick` 에서 바로 열면
같은 클릭의 잔여 이벤트에 곧바로 닫힌다. `setTimeout(zenOpen, 40)` 로 연다.

### (8) SVG 에는 `hidden` 프로퍼티가 없다 ★ v5.7 에서 30분 날림
`hidden` 은 `HTMLElement` 의 것이다. **`svgEl.hidden = false` 는 속성을 지우지 않고**
그냥 JS 객체에 값 하나를 얹을 뿐이라, `[hidden]{display:none}` 에 계속 걸려 안 보인다.
`el.hidden` 을 읽으면 `false` 인데 `el.hasAttribute("hidden")` 은 `true` 인 기묘한 상태가 된다.
SVG 는 `removeAttribute("hidden")` / `setAttribute("hidden","")` 로만 껐다 켤 것.

### (9) rem 이 16px 이 아니다
글자 크기 설정이 루트 폰트 크기를 바꾸므로 **`1rem` 이 20px 을 넘을 수 있다.**
전체 화면을 채우는 것(대기 화면 등)은 rem 만으로 크기를 잡으면 큰 글씨 설정에서 넘친다.
`min(22rem, 30vw, 52vh)` 처럼 **vw·vh 를 같이 물려서** 창 밖으로 안 나가게 할 것.

### (10) CSS 변수 이름
바탕색은 `--ink` 다. **`--bg` 라는 변수는 없다.** `var(--bg)` 를 쓰면 조용히 투명이 되어
전체 화면 오버레이가 뒤를 다 비춘다 (v5.5 에서 실제로 겪음).

### (11) 테스트 착시 — 진짜 버그로 오인한 것들
- `p.evaluate(()=>el.click())` 은 mousedown 이 없어서 바깥클릭 핸들러가 안 돈다.
- 첫 실행에는 프로필 선택 화면이 떠서 `#list` 가 가려져 있다.
- 좁은 화면(520px 미만)에서는 사이드바가 접혀 `#wishBtn`·`#statsBtn` 이 안 보인다 →
  `offsetParent` 로 확인하고 없으면 `.click()` 직접 호출.
- 오늘이 주말이면 `mode:"wd"` 항목은 체크 줄에 아예 안 뜬다.
- 520px 미만은 **위젯 모드**라 사이드바가 통째로 없다. `#dateLine`·`#moreBtn` 대신
  `#wDateBtn`·`#wMore` 를 쓴다. `document.body.classList.contains('is-widget')` 로 갈라야 한다.

### (13) 사이드바 원형 차트는 JS 가 크기를 잡는다 (v5.9)
`.ring` 의 폭은 `var(--ringpx, ...)` 이고 그 값을 `sizeRing()` 이 매 `render()` 마다 넣는다.
CSS 미디어쿼리에 `.ring{width:...}` 를 다시 쓰면 **JS 값을 덮어버린다** (실제로 한 번 덮었다).
크기를 손보려면 `sizeRing()` 안의 최소·최대(`3.4rem` ~ `16rem`)를 고칠 것.
재는 순서도 중요하다 — 링을 `0px` 로 만들어 두고 형제(시계·카테고리 막대·몸무게)를 먼저 잰다.
`.side` 에 붙인 ResizeObserver 가 `sizeRing()` 을 다시 부르는데,
**링 자신을 관찰하면 무한 반복이 된다.** 관찰 대상은 반드시 `.side`.

### (12) 절대 하지 말 것
- 확인 없이 "데이터가 날아갔다"고 단정하기. 2026-09-10 에 사용자가
  "언젠가가 날아갔다"고 한 건 **화면의 언젠가 줄이 안 보인다**는 뜻이었고 데이터는 멀쩡했다.
  밤새 헛짓했다. 먼저 물어볼 것: "사이드바 언젠가 숫자가 몇인가요?"

---

## 4. 기능 지도 — 어디를 고치면 되나

| 기능 | 핵심 함수 / 엘리먼트 |
|---|---|
| 한 줄 입력 파싱 | `parseLine`, `splitTokens`, `eatDate` |
| 자동완성 | `attachSuggest`, `suggestIndex` — 목록이 **위로** 열려서 ↑↓ 의미가 뒤집혀 있다 |
| 카드 그리기 | `makeRow`, `catChip`, `seqChip`, `statusOf`, `levelOf` |
| 회차 | `seq`/`seqTot`, `topSeqOf`, `canNext`, `makeNext` |
| 언젠가 | `SOMEDAY`, `wishes`, `wishRow`, `renderWishSheet`, `pullWish`, `sendToWish` |
| 보드 옮기기 | `otherProfile`, `mapCatTo`, `sendToBoard` |
| 작품 기록 | `WKINDS`, `works()`, `workOf`, `setWork`, `dropWork`, `openWork`, `workTally`, `retireWish` |
| 성공·실패 | `habits()`, `hres()`, `setHab`, `habApplies`, `fillHabits`, `openWhy` |
| 달력 | `dayStat`, `drawCal`, `placeCal` — `#calPop` z-index **19** (오버레이 20 아래) |
| 통계 | `renderStats` → `renderWishStats` / `renderWorkStats` / `renderHabStats` / `renderDetail` |
| 통계 보드 전환 | `stBoard`, `stCache`, `sd()`, `stCat()` — 다른 보드는 사본을 읽어 **보기만** 함 |
| 동기화 | `payload`, `mergeAll`, `addOnly`, `mergeWorks`, `mergeRemote`, `pushNow`, `pullNow` |
| 하루 시작 시각 | `logicalNow`, `todayKey`, `dayStart` |
| 업데이트 기록 | `CHANGES` 배열, `openLog`, `verLog`/`verLogAdd` |
| 대기 화면 | `zenOpen`/`zenClose`/`zenPaint`/`zenPaintCal`, `idleKick`, `IDLE_KEY` · 여는 곳 `#zenBtn`·`#wZen`·`#clock`·`#mZen` |
| 시계 모양 | `clockFace`/`clockSec`/`clock12`, `clockText`, `buildDial`/`paintDial`, `zenFace`, `FACE_KEY`·`COPT_KEY` |
| 대기 화면 배치 | `.zwrap`(세로) > `.zbox`(가로: `.zleft` + `.zcal`) · 860px 미만이면 세로로 쌓임 |
| 원형 차트 | `renderRing`(그리기) + `sizeRing`(크기) · 가운데 글자는 `cqw` 로 링을 따라간다 |
| 카드 배치 | `layoutList`, `ROWSIZES`(고정 rem), `ROWFIX`(줄 수), `fitRows`, `#rowSeg`/`#rowSeg2`/`#colSeg` |
| 디버그 창구 | `window.QB` = store, save, render, mergeRemote, payload, openWork, setWork, workRows, hres … |

---

## 5. 테스트

Playwright 로 실제 브라우저를 띄워 확인한다. 크로미움은 `/opt/pw-browsers/chromium`.

```js
import pw from '/home/claude/.npm-global/lib/node_modules/playwright/index.js';
const b = await pw.chromium.launch({ executablePath:'/opt/pw-browsers/chromium' });
// localStorage 에 데이터를 심고 reload 한 뒤 검사하는 방식
```

현재 테스트 파일 (`/home/claude/board/`):

| 파일 | 내용 |
|---|---|
| `t26` | 자동완성 키보드 조작 |
| `t28` | 통계 보드 전환 · 반복 횟수 |
| `t30` | 언젠가 탭 개편 |
| `t34` | 언젠가 유실 경로 추적 |
| `t35` | 언젠가 방어막 · 보드 옮기기 |
| `t37` | 언젠가 줄 표시 (오늘/지난 날) |
| `t39` | 버전 문지기 |
| `t40` | 작품 기록 (엔딩/완주) · 취미 통계 |
| `t41` | 작품 화면 3폭×테마 |
| `t42` | 업데이트 기록 · 여러 줄 추가 제거 확인 |
| `t43` | 실패·예외 사유 |
| `t44` | 금액 기입 · 지출 통계 |
| `t45` | v5.4 — 취미 탭이 일감을 끌어오지 않는지 |
| `t46` | v5.5 — 날짜·요일 표시 · 대기 화면 |
| `t47` | v5.6 — 카드 높이 줄 수 고정 · 시계·달력 버튼 |
| `t48` | v5.7 — 시계 모양(디지털/아날로그/둘 다 · 초 · 12시간) |
| `t49` | **v5.8 — 대기 화면 가로 배치 · 9가지 창 크기에서 스크롤 없는지** |
| `t50` | **v5.9 — 원형 차트 크기·중앙 정렬 · 11가지 창 + 집 보드(몸무게 상자)에서 안 넘치는지** |

**배포 전 반드시** 다크/라이트 × 1400px/420px 조합으로 눈으로 확인할 것.
기본 설정만 보고 넘기면 사용자 설정에서 터진다 (2026-09-08 태그 실종 사고).

문법 검사:
```bash
python3 -c "
s=open('index.html',encoding='utf-8').read()
i=s.index('<script>\n(function(){'); j=s.index('</script>',i)
open('/tmp/chk.js','w',encoding='utf-8').write(s[i+8:j])
" && node --check /tmp/chk.js
```

---

## 6. 버전 히스토리 요약

소스 안의 `CHANGES` 배열이 정본이다 (앱에서 왼쪽 아래 버전을 누르면 보인다). 최근 것만:

- **v5.9** 사이드바 원형 차트를 남는 자리만큼 키우고 가운데 정렬 (`sizeRing`)
- **v5.8** 대기 화면 가로 배치(시계 왼쪽·달력 오른쪽), 창 높이에도 크기를 묶어 스크롤 제거
- **v5.7** 시계 모양 설정(디지털/아날로그/둘 다, 초 on·off, 12시간)
- **v5.6** 카드 높이를 줄 수로도 고정 · 메인에 시계·달력 버튼 · 대기 화면 확대
- **v5.5** 날짜 옆 요일 크게 · 대기 화면(시계·달력, 방치 자동 전환)
- **v5.4** 취미 통계가 일감을 끌어오던 것 수정. 이름 추측 제거, 등록제로 전환, 빼기 버튼
- **v5.3** 실패·예외에 금액 기입 + 지출 통계 (합계·평균·사유별)
- **v5.2** 실패·예외 사유 묻기 + 자주 쓴 사유 버튼 + 통계 사유 목록
- **v5.1** 업데이트 기록 화면, 여러 줄 한번에 추가 삭제
- **v5.0** 작품 기록 (게임 엔딩 / 드라마 완주 / 책 완독) + 통계 취미 탭
- **v4.15** 앱 버전 문지기
- **v4.14** 하루 시작 시각 설정 · 언젠가 줄 표시 수정
- **v4.13** 언젠가 유실 경로 3개 차단 + 되살리기 · 보드 옮기기
- **v4.12** index.html 만 올려도 반영 · 새 버전 알림

---

## 7. 남은 일 (사용자와 합의된 것)

우선순위 순:

1. **언젠가를 `store.days` 밖으로 분리** (`store.wish`). 유실 사고 세 건의 공통 뿌리.
   지금은 방어막으로 막아둔 상태. 마이그레이션 필요.
2. **자동 백업.** 현재 `questboard:wishbak:` 은 언젠가만, 로컬만.
   하루 한 번 전체 스냅샷 3~5개 돌려 남기고 "어제 상태로" 되돌리기.
3. **월별 요약 장부** (`store.sum`). 데이터가 2년 가까워지면 Gist 1MB 천장에 닿는다.
   오래된 날은 "9월: 완료 82 / 미룸 14" 요약만 남기고 원본을 접는 방식.
4. **공휴일 표 2029년 이후 연장.** 지금 `HOLIDAYS` 는 2026~2028 만 있다. 한 줄 작업.
5. 보드 옮기기가 날짜를 그대로 들고 간다 — 옮길 때 오늘/내일을 고르게 하면 손이 덜 간다.

**하지 말 것으로 합의된 것:** 창 크기 고정, 항상 위 고정, AI 토큰 잔여량 표시(포기함).

---

## 8. 사용자 맥락 (알아두면 좋은 것)

- 자정을 넘겨 어제 보드를 정리하는 습관이 있다. 날짜 관련 버그가 여기서 여러 번 나왔고
  v4.14 의 "하루 시작 시각" 설정이 그 뿌리를 없앤 것이다.
- 폰을 거의 안 켠다 (출퇴근 때 PC 에서 먼저 실행). 그래서 기기 간 버전이 크게 벌어진다.
  → 버전 문지기가 필요했던 이유.
- 성공·실패 항목 3개: `출근 버스로 하기`(평일만), `퇴근 자전거 30분 컷`(평일만),
  `돈 안쓰기`(매일, 세부 기준 "출퇴근 비용은 제외"). 본인이 가혹한 항목이라고 함.
- 취미는 게임·드라마 위주. 언젠가 목록에 작품을 담아두고 그날그날 보드에 직접 적어서 한다
  ("오늘로" 버튼은 잘 안 씀). 그래서 통계가 **이름 기준**으로 도는 것이다.
- 공개 저장소라 소스에 개인정보를 넣지 않는다. 이름·부서는 `questboard:who:<보드>` 로
  그 기기 localStorage 에만 둔다 (v4.5).
